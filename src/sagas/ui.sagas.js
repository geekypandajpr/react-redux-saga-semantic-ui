import {call,put} from 'redux-saga/effects';

import { SIDEMENU_TOGGLE } from '../constants/action';
import { sideMenuToggle } from '../actions/ui.actions';

// Log in service saga
export function* sideMenuToggleRequest(action){
            // yield put({type:SIDEMENU_TOGGLE,data:'maan'});

}
